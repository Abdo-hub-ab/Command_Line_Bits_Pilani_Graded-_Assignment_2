Q10
---------------------------------

1. Program Creation

1.1 Command: nano signal_handling.c
Explanation: I opened nano editor to create and write the C program file signal_handling.c.

-------------------------------------------------

1.2 Command: C Program

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <signal.h>
#include <sys/wait.h>

volatile sig_atomic_t got_sigterm = 0;
volatile sig_atomic_t got_sigint = 0;

void handle_sigterm(int sig) {
    (void)sig;
    got_sigterm = 1;
}

void handle_sigint(int sig) {
    (void)sig;
    got_sigint = 1;
}

int main() {
    printf("Parent running. PID: %d\n", getpid());

    struct sigaction sa1, sa2;
    sa1.sa_handler = handle_sigterm;
    sigemptyset(&sa1.sa_mask);
    sa1.sa_flags = 0;

    sa2.sa_handler = handle_sigint;
    sigemptyset(&sa2.sa_mask);
    sa2.sa_flags = 0;

    sigaction(SIGTERM, &sa1, NULL);
    sigaction(SIGINT, &sa2, NULL);

    pid_t child1 = fork();
    if (child1 == 0) {
        sleep(5);
        kill(getppid(), SIGTERM);
        exit(0);
    }

    pid_t child2 = fork();
    if (child2 == 0) {
        sleep(10);
        kill(getppid(), SIGINT);
        exit(0);
    }

    while (1) {
        if (got_sigterm) {
            printf("Parent received SIGTERM. Exiting gracefully.\n");
            break;
        }
        if (got_sigint) {
            printf("Parent received SIGINT. Exiting gracefully.\n");
            break;
        }
        sleep(1);
    }

    while (wait(NULL) > 0) {}

    return 0;
}

Explanation: I wrote a C program demonstrating signal handling:
 Parent process runs indefinitely.
-  One child sends SIGTERM after 5 seconds.
- Another child sends SIGINT after 10 seconds.
- Parent handles each signal differently and exits gracefully.
-Parent waits for child processes to finish.

------------------------------------------------
2. Compile Program

2.1 Command: gcc signal_handling.c -o signal_handling
Explanation: I compiled the C program using gcc to generate an executable named signal_handling.

-------------------------------------------------

3. Run Program

3.1 Command: ./signal_handling
Explanation: I ran the program to observe the parent handling signals sent by child processes
